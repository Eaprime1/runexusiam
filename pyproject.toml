[project]
name = "SDWG"
version = "0.1.0"
description = "SDWG of your project"
readme = "README.md"


# Define dependencies as an array of strings, not as key-value pairs
dependencies = [
    "numpy>=1.21.0",
    "pandas>=1.3.0",
    "pyyaml>=5.4.1",
    "typing-extensions>=4.0.0"
]
